\begin{figure}[htb]
    \centering
    \begin{lstlisting}
Applicative:
    f g x => (f(g))(x) 

Compositional:
    f g x => f(g(x))
\end{lstlisting}
    \caption{A forth program that sums all the elements on the stack}
    \label{fig:forth_example}
\end{figure}
